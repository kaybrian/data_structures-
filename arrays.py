new_list = [1, 2, 3]
result = new_list[0]

# searching for an element in arrays
if 1 in new_list:
    print(True)


for n in new_list:
    if n == 1:
        print(True)
    break

# inserting values in an array
numbers = []
numbers.extend([4, 5, 6, 7, 8])

print(numbers)


# Building our own data structure (linked lists)
